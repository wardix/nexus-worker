{
  "name": "nexus-worker",
  "version": "1.0.0",
  "description": "Next-generation NATS worker",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "start": "bun run src/main.ts",
    "format": "biome format --write .",
    "lint": "biome lint --write --unsafe .",
    "check": "biome check --write --unsafe ."
  },
  "dependencies": {
    "nats": "^2.28.2",
    "pino": "^9.4.0",
    "pino-pretty": "^11.2.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "latest",
    "@types/node": "^22.5.4",
    "typescript": "^5.0.0"
  }
}
